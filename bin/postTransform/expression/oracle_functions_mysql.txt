# MySQL 특화 Oracle Function 변환 목록
# MySQL에서 변환이 복잡하거나 특별한 처리가 필요한 함수들

# 날짜/시간 함수들 (MySQL 문법으로 변환 필요)
SYSDATE|SYSDATE
TO_CHAR|TO_CHAR\s*\(
TO_DATE|TO_DATE\s*\(
ADD_MONTHS|ADD_MONTHS\s*\(
MONTHS_BETWEEN|MONTHS_BETWEEN\s*\(
LAST_DAY|LAST_DAY\s*\(
NEXT_DAY|NEXT_DAY\s*\(
EXTRACT|EXTRACT\s*\(
SYSTIMESTAMP|SYSTIMESTAMP

# NULL 처리 함수들 (MySQL 문법 차이)
NVL2|NVL2\s*\(
NVL|NVL\s*\(

# 문자열 함수들 (MySQL 문법 차이)
SUBSTR|SUBSTR\s*\(
INSTR|INSTR\s*\(
INITCAP|INITCAP\s*\(
TRANSLATE|TRANSLATE\s*\(

# 수학 함수들
TRUNC|TRUNC\s*\(
TO_NUMBER|TO_NUMBER\s*\(
CEIL|CEIL\s*\(
SIGN|SIGN\s*\(
GREATEST|GREATEST\s*\(
LEAST|LEAST\s*\(

# 문자열 연결 (MySQL에서 CONCAT 사용)
STRING_CONCAT|\|\|

# DUAL 테이블 (MySQL에서 불필요)
DUAL_TABLE|FROM DUAL

# 외부 조인 (MySQL 문법 차이)
OUTER_JOIN|(\+)
DYNAMIC_OUTER_JOIN|<if.*(\+).*</if>

# Oracle 특화 함수들
ROWID|ROWID
SYS_GUID|SYS_GUID\s*\(

# JSON 함수들 (MySQL 8.0+ 지원)
JSON_VALUE|JSON_VALUE\s*\(
JSON_QUERY|JSON_QUERY\s*\(
JSON_OBJECT|JSON_OBJECT\s*\(
JSON_ARRAY|JSON_ARRAY\s*\(

# 전문 검색 관련 (MySQL 구현 다름)
CONTAINS|CONTAINS\s*\(
SCORE|SCORE\s*\(
CATSEARCH|CATSEARCH\s*\(

# XML 관련
XMLAGG|XMLAGG\s*\(

# 집계 함수 (MySQL 문법 차이)
GROUP_CONCAT|GROUP_CONCAT\s*\(

# 분석 함수들 (MySQL 8.0+ 지원하지만 문법 차이)
FIRST|FIRST\s*\(
LAST|LAST\s*\(
KEEP|KEEP\s*\(
DENSE_RANK_FIRST|DENSE_RANK\s+FIRST
DENSE_RANK_LAST|DENSE_RANK\s+LAST

# DML 구문 (MySQL 문법 차이)
MERGE_STATEMENT|MERGE\s+INTO
USING_CLAUSE|USING\s+\(
WHEN_MATCHED|WHEN\s+MATCHED
WHEN_NOT_MATCHED|WHEN\s+NOT\s+MATCHED
